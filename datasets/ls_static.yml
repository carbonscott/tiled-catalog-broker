# AUTO-GENERATED by dcs inspect on 2026-04-30
# Source: /tmp/ls_static
# Files scanned: 1 HDF5 (S_52.h5)
# Entities detected: 1 (one per file)

# === REQUIRED: Fill in the label ===
# The machine-readable `key` is auto-filled at registration as
# slug(label) — e.g., 'Broad Sigma' -> 'BROAD_SIGMA'. Don't author it.
label: "LCLS RIXS Static"            # TODO: human-readable name (e.g., Broad Sigma)

# === REQUIRED: Dataset container metadata ===
# These fields describe the dataset as a whole and enable
# cross-dataset queries like client.search(Key('method') == 'RIXS')
metadata:
  method: ['RIXS']
  data_type: "experimental"
  material: "NiPS3"
  producer: "PSANA"
  project: "MAIQMag"
  experiment: "rix100818424"
  facility: "LCLS"
  description: "qRIXS scans from experiment rix100818424"

# === Auto-detected ===
data:
  directory: /sdf/data/lcls/ds/prj/prjmaiqmag01/results/LS/static
  server_base_dir: /prjmaiqmag01/LS/static
  file_pattern: "*.h5"
  layout: per_entity

# === Per-entity parameters lifted from HDF5 root attributes ===
# (twotheta, theta, chi, phi, x, y, z, dety — diffractometer/sample geometry)
parameters:
  location: root_attributes

# === TODO: Classify these arrays as artifacts or shared axes ===
# Move each entry to either 'artifacts:' or 'shared:' section
# Artifacts = output observables (different per entity)
# Shared = axes/grids (same across all entities)
artifacts:
- type: I0
  dataset: /I0
    # shape: (1, 21), dtype: float64
    # range: [49.79719757865875, 83.67377339973233]
- type: I_rn
  dataset: /I_rn
    # shape: (1, 21, 1031), dtype: float64
    # range: [0.0, 16.99680610886323]
- type: S
  dataset: /S
    # shape: (1, 21, 1031), dtype: float64
    # range: [0.0, 1422.186902872208]
- type: counts
  dataset: /counts
    # shape: (1, 21), dtype: float64
    # range: [3917875.0, 3983525.0]
- type: energy
  dataset: /energy
    # shape: (1031,), dtype: float64
    # range: [-4.9858, 14.9962]
- type: motor
  dataset: /motor
    # shape: (21,), dtype: float64
    # range: [857.3, 861.3]
- type: motor_step
  dataset: /motor_step
    # shape: (21,), dtype: float64
    # range: [1.0, 21.0]
- type: pixel
  dataset: /pixel
    # shape: (1031,), dtype: int32
- type: rn
  dataset: /rn
    # shape: (1,), dtype: float64
    # range: [52.0, 52.0]


# === Provenance (optional) ===
# These fields are stored on the dataset container for tracking
# how and when data was generated.
provenance:
  created_at: "2026-04-30"
  code_github: "https://github.com/mpmdean/qRIXS_data_analysis"   # version of generating code
  code_commit: "353d64a"    # git hash of generating code

# === Recommendations for data producer ===
# - No 'created_at' timestamp — add as HDF5 root attribute
# - No 'generator' or 'code_version' — add as HDF5 root attribute
# - No 'material' identifier — add as HDF5 root attribute

# === Consistency check: PASSED (1 files checked) ===
key: LCLS_RIXS_STATIC
