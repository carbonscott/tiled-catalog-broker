# AUTO-GENERATED by dcs inspect on 2026-04-30
# Source: /tmp/ls_tr
# Files scanned: 1 HDF5 (S_139_140.h5)
# Entities detected: 6 (1 files x 6 batch size)

# === REQUIRED: Fill in the label ===
# The machine-readable `key` is auto-filled at registration as
# slug(label) — e.g., 'Broad Sigma' -> 'BROAD_SIGMA'. Don't author it.
label: "RIXS Differential Map"

# === REQUIRED: Dataset container metadata ===
# These fields describe the dataset as a whole and enable
# cross-dataset queries like client.search(Key('method') == 'RIXS')
metadata:
  method: ['RIXS']
  data_type: "experimental"
  material: "NiPS3"
  producer: "PSANA"
  project: "MAIQMag"
  experiment: "rix100818424"
  facility: "LCLS"
  description: "qRIXS scans from experiment rix100818424"

# === Auto-detected ===
data:
  directory: /sdf/data/lcls/ds/prj/prjmaiqmag01/results/LS/time_resolved
  server_base_dir: /prjmaiqmag01/LS/time_resolved
  file_pattern: "*.h5"
  layout: per_entity

# === Per-entity parameters lifted from HDF5 root attributes ===
# (twotheta, theta, chi, phi, x, y, z, dety — diffractometer/sample geometry)
parameters:
  location: root_attributes

# === Per-entity artifacts (whole .nc file collapses into one entity) ===
# (with 1 entity per file there's nothing to share across entities, so
# everything — including the energy/motor/pixel axes and the rn run-number
# array — is stored inside the entity as an artifact)
artifacts:
- type: S
  dataset: /S
    # shape: (6, 31, 597), dtype: float64, range: [0.0, 99.18417550016474]
- type: I_rn
  dataset: /I_rn
    # shape: (6, 31, 597), dtype: float64, range: [0.0, 4.754541572148227]
- type: I0
  dataset: /I0
    # shape: (6, 31), dtype: float64, range: [12.904879221550292, 22.232469599727846]
- type: counts
  dataset: /counts
    # shape: (6, 31), dtype: float64, range: [599300.0, 663975.0]
- type: energy
  dataset: /energy
    # shape: (597,), monotonic, range [-0.4074, 11.155000000000001], step=0.0194
- type: motor
  dataset: /motor
    # shape: (31,), monotonic, range [-1.3077181990907238, 1.6922818009092764], step=0.1
    # — lxt (pump–probe delay, ps)
- type: motor_step
  dataset: /motor_step
    # shape: (31,), monotonic, range [1.0, 31.0], step=1
- type: pixel
  dataset: /pixel
    # shape: (597,), monotonic, range [1604.0, 2200.0], step=-1
    # — detector pixel indices (ROI)
- type: rn
  dataset: /rn
    # shape: (6,), dtype: float64, range: [139.0, 144.0]
    # — run numbers


# === Provenance (optional) ===
# These fields are stored on the dataset container for tracking
# how and when data was generated.
provenance:
  created_at: "2026-04-30"
  code_github: "https://github.com/mpmdean/qRIXS_data_analysis"   # version of generating code
  code_commit: "353d64a"    # git hash of generating code

# === Recommendations for data producer ===
# - No 'created_at' timestamp — add as HDF5 root attribute
# - No 'generator' or 'code_version' — add as HDF5 root attribute
# - No 'material' identifier — add as HDF5 root attribute

# === Consistency check: PASSED (1 files checked) ===
key: RIXS_DIFFERENTIAL_MAP
